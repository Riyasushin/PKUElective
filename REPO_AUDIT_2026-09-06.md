# 问题与修复状态

记录日期：2026-09-06。最初通过浏览器页面和代码排查问题，随后直接请求服务器，并用机器学习完成真实补选。本文更新到这次修复后的状态；原始排查过程可在 Git 历史中查看。

## 网站改了什么

原来的补退选入口 `SupplyCancel.do`、后续页 `supplement.jsp` 和每页 20 条的分页公式仍然可用。课程名、班号、开课单位、限数/已选等关键列也仍在，主要故障来自程序本身。

浏览器还显示单独的 `SupplyOnly.do` 入口，程序没有覆盖它；不能直接用它替换补退选入口。原浏览器快照中的第 3 页位置也不可靠：后来直接查服务器，在第 1 页找到测试目标。

当时页面的候补阶段为 9 月 4 日 15:00 至 9 月 5 日 10:00；先到先得阶段为 9 月 6 日 10:00 至 9 月 9 日 12:00；跨院系阶段为 9 月 9 日 17:00 至 9 月 15 日 10:00。均为 2026 年该学期时间，不应写死为后续学期默认值。

## 已修复

| 问题 | 修改后的行为 |
| --- | --- |
| 代理返回字符串，调用方却读取 `.code` | 统一返回 `Captcha` |
| 重复请求、旧投票残留、复用关闭的连接 | 每张图片一次请求，不保留跨图片状态 |
| 识别类型被覆盖、部分超时写死 | 使用配置中的类型和超时，走 HTTPS |
| 验证码无限循环 | 增加次数上限，遇到未知校验状态报错 |
| 补选请求被网络层自动重试 | 提交接口零重试；`--once` 先查结果再判断 |
| 链接拼接只支持一种形式 | 支持相对链接，限制为本站补选路径 |
| 严格依赖 class 和首个文字节点 | 解析兼容多个 class、嵌套文字、缺少链接 |
| 没有简便的只读及单次运行入口 | 新增 `--check` / `--once` |
| 成功提示不足以证明选上 | `--once` 查询明确状态，候补不算成功 |
| 登录失败时打印密码 | 删除该输出；完整请求日志脱敏仍未完成 |
| 样例 burst 行内注释导致读取失败 | 注释移到独立行；解析器的静默回退仍需改 |

## 配置的实际含义

常用配置和示例见 [README](README.md)。以下是容易误解的部分：

| 配置 | 实际含义 |
| --- | --- |
| `course:*` | 后缀只是用户标识；按名称、班号、院系匹配，不按课程号匹配 |
| `supply_cancel_page` | 只读取这个可选列表页；没有自动发现目标页的功能 |
| `refresh_interval` | 一轮结束后的等待基准，不是两次请求之间的固定间隔 |
| `random_distribution = normal` | 当前样例默认。8 秒、偏移 0.2 对应标准差 1.6 秒，不是固定落在 6.4–9.6 秒 |
| `random_distribution = poisson` | 实际用指数分布；截断前均值为 interval × deviation。8 和 0.2 得到 1.6 秒，不是 8 秒 |
| `burst_*` | 仅 burst 且 deviation > 0 时使用；代码还有超出配置范围的额外等待 |
| `elective_client_pool_size` | 可用会话数，不是并发补选数；“同 IP 最多 5”仅是旧注释，未重新核实 |
| `elective_client_max_life` | 程序设定的会话寿命；`-1` 不主动按年龄过期，但服务器仍可能让会话失效 |
| 两个客户端 timeout | 请求超时，不是包含全部重试的总运行期限 |
| `login_loop_interval` | 登录循环基础等待，退避及重启逻辑还会增加等待 |
| `mutex:*` | 轮询中让几个目标只选一个，不会替换或退掉已选课程 |
| `delay:*` | 有余量且剩余名额 <= threshold 才尝试，可能刻意错过早期空位 |
| `monitor` | 仅轮询加 `-m` 时启用；host/port 控制状态服务地址 |
| `notification` | 默认关闭。实际服务是 `push.jwks123.cn`，并非旧 README 所写的 sre24；未重新测试 |
| `debug_*` | 打开会输出或保存请求细节，仍可能包含认证信息 |
| `print_mutex_rules` | 只改变规则日志的详细程度 |
| apikey 的 `RecognitionTypeid` / `Timeout` | 现在分别控制识别类型和请求超时，均实际生效 |
| `user_agents.user.txt` | 可选，覆盖打包的 User-Agent 列表 |

## 还没解决

1. **跨页目标和稳定匹配**：没有自动翻页寻找目标，也未用课程号做匹配。目标不在配置页时，旧轮询会误报“不在选课计划”。
2. **两个运行模式尚未统一**：`--once` 按表头取表、核对明确选课状态；旧轮询仍有位置和文案假设，候补阶段需要单独改造和测试。
3. **错误恢复**：旧轮询的连续登录失败重启逻辑会打断更长的冷却策略，工作线程退出也不保证主进程退出。
4. **配置校验**：部分非法值仍会被宽泛的 except 吞掉并回退默认值；未知随机分布名也没有明确报错。
5. **隐含等待**：burst 先进入休息，还可能额外等待 300–480 秒；刷新参数不能完整描述实际耗时。
6. **通知和日志**：通知间隔按 finally 更新时间，不是“上次成功发送”；完整请求日志仍需脱敏。
7. **覆盖范围**：双学位、候补、跨院系权限、长期轮询还需分别验证。

已通过的测试和实际输出见[实测记录](RUN_VALIDATION_2026-09-06.md)。一次补选成功证明这条具体流程可用，不等于以上问题都已解决。
