# 仓库与当前选课页面核对

> 后续更新：本文件保留修复前审计记录。现已修复并完成机器学习真实选课，最新状态和验证边界见 [运行验收](RUN_VALIDATION_2026-09-06.md)。

核对日期：2026-09-06。代码依据本地工作树；页面依据 Safari 已登录的“补选退选”页面的可访问性树。未提交补选、退选、验证码或第三方识别请求。公共网页抓取失败，浏览器页面读取成功。页面内容是当时显示的快照，名额不是持续实时数据。

## 1. 仓库实际做什么

`main.py → cli.py` 读取 `-c/--config`，启动登录线程、选课线程；`-m` 额外启动 Flask 状态服务。

- `iaaa.py`：统一认证；`elective.py`：SSO、补退选列表、验证码验证和补选请求。
- `client.py`：requests Session、Cookie、超时及重试。
- `loop.py`：会话池、轮询、课程匹配、互斥/延迟规则、调用验证码识别和提交。
- `parser.py`：lxml 解析表格；`course.py`：以课程名、班号、开课单位组成匹配标识。
- `captcha/proxy.py`：当前真正调用的识别实现，向三个识别类型发请求后加权投票；`online.py` 提供凭据读取、图片转换和另一个同步识别实现。
- `hook.py`：根据标题和提示文案分类异常；`notification/` 推送；`monitor.py` 提供只读状态。

这是补退选自动化工具，不包含全站课程检索和自动添加选课计划。目标必须已在选课计划中。现有代码每轮只读取一个配置页，不支持同一配置中的目标散落在多个可选列表页。

本地只有 `config.sample.ini` 和 `apikey.sample.json`，未见实际 `config.ini`、`apikey.json`；因此本次评估的是字段用途，不能判断用户真实目标配置是否正确。README 的版本说明主要停留在 2022，IMPLEMENTATION_GUIDE 可帮助理解架构，但不能替代实际代码及在线验证。

## 2. 当前页面已核实的兼容性

用户提供的地址路径为 `/elective2008/edu/pku/stu/elective/controller/supplement/supplement.jsp`。

| 项目 | 当前页面 | 仓库判断 |
| --- | --- | --- |
| 补退选入口 | `supplement/SupplyCancel.do?xh=…` | 与 `ElectiveURL.SupplyCancel` 一致 |
| 后续页 | `supplement/supplement.jsp` | 与 `ElectiveURL.Supplement` 一致 |
| 分页参数 | `netui_pagesize=electableListGrid;20`、`netui_row=electableListGrid;40` | `(page-1)*20` 正好得到第 3 页；页面也显示 Page 3 of 3 |
| 选课结果 | `electiveWork/showResults.do` | 与现有常量一致 |
| 另一个“补选”入口 | `supplement/SupplyOnly.do` | 现有代码没有覆盖；不能直接替换补退选入口 |
| 目标字段 | 课程名、班号、开课单位 | 与配置及解析器的语义一致 |
| 名额字段 | 限数/已选，例如 `50 / 5` | 应解释为上限 50、已选 5，剩余 45；空格可被 `int()` 处理 |
| 操作字段 | 可选表为“补选”；已选表为“退选”，另有“选课状态” | 可以用表头区分表用途，无需依赖位置 |
| 其他字段 | 课程号、课程类别、学分、周学时、教师、年级、上课/考试信息、自选 P/NP | 已有代码按列名定位，增加这些列本身不会导致列号错位 |

页面明确区分三阶段：候补阶段 9 月 4 日 15:00 至 9 月 5 日 10:00；先到先得阶段 9 月 6 日 10:00 至 9 月 9 日 12:00；跨院系阶段 9 月 9 日 17:00 至 9 月 15 日 10:00。阶段能力应进入状态模型，不宜只凭“有余量”推断可提交；这些是本学期页面时间，不能写死为长期默认配置。

当前证据仅覆盖可见结构和导航链接。HTML 的 class、嵌套层级、操作单元格的 href/onclick、隐藏参数及 JS 请求没有取得；故不能声称现有 XPath 或提交接口已通过新版验证。验证码图片接口、验证接口的返回值、SSO 参数及成功提示也仍需真实请求证据。

## 3. 哪些配置有用

| 字段 | 实际用途与建议 |
| --- | --- |
| `user.student_id/password` | 登录必需；`student_id` 也用作列表/验证请求的 `xh` |
| `dual_degree/identity` | 双学位切换用；当前代码即使关闭双学位也读取并校验 identity，保留合法的 `bzx` 默认值 |
| `supply_cancel_page` | 当前最直接影响目标能否找到的字段。用户给的第 3 页对应值 3；仅当目标确在这一页时使用，不能因为正在浏览第 3 页就认定所有目标在此 |
| `course:*` 的 `name/class/school` | 核心目标，须对应页面文字；班号转整数，因此 01 与 1 相同。配置先后顺序决定同轮优先级 |
| `refresh_interval/random_deviation/random_distribution` | 均有调用，但指数分布语义有误，见下文；刷新间隔只是循环末尾等待，不包含请求、额外 sleep、重试和验证码时间 |
| 六个 `burst_*` | 仅 burst 且 deviation > 0 时参与等待；样例行内注释会使数值读取失败，再静默回退默认值 |
| 两个 `*_client_timeout` | 对应网络请求超时，有效；不能视为包含所有重试的总截止时间 |
| `elective_client_pool_size/max_life` | 会话数量与本地过期时间，有效；并非并行选课线程数。样例“同 IP 最多 5”是历史注释，本次未核实当前限制 |
| `login_loop_interval` | 登录循环基础等待，有效，但额外抖动、退避和重启逻辑会改变实际等待 |
| `print_mutex_rules` | 仅影响规则日志详细程度 |
| `debug_print_request/debug_dump_request` | 有效的诊断开关，但现有输出未脱敏，含请求头/请求体和会话信息；应先修日志脱敏 |
| `monitor.host/port` | 仅带 `-m` 时启动服务使用；默认保持本机地址即可 |
| `notification.disable_push/token/verbosity/minimum_interval` | 有实际调用；verbosity 在 logger 中使用。README 的 sre24 描述与实际 `push.jwks123.cn` 不一致，服务可用性未验证；间隔时间戳实际在 finally 更新，不符合“上次成功发送”语义 |
| `mutex:*` | 已选中任一目标后忽略互斥目标；不会自动退掉已有课程 |
| `delay:*` | 有效，但为“有余量且剩余名额 <= threshold 才提交”，可能刻意推迟机会，普通目标通常不需要 |
| apikey 的 `username/password` | 当前识别路径必需，启动时即读取；不启动主循环也应可做纯配置检查，现架构尚未分离 |
| apikey 的 `Timeout` | 并行代理实际使用；同步识别实现仍写死 20 秒 |
| apikey 的 `RecognitionTypeid` | 被要求存在且转整数，但并行代理随后用 `[3,1003,7]` 覆盖，无法控制实际方法选择 |
| `user_agents.user.txt` | 可覆盖打包 UA 列表；属于可选资源配置，非选课网站页面参数 |

`pyproject.toml` 和 `uv.lock` 管理 Python 环境，不承载学号、课程或选课位置。README 将 apikey 的复制步骤误写成 `.ini`，实际必须是 `.json`，且 JSON 样例说明漏掉两个代码必需字段。

## 4. 更新优先级和实现位置

### P0：先修确定的运行阻断和错误行为

1. **识别返回类型不匹配**：`captcha/proxy.py:recognize()` 返回字符串，`loop.py` 识别后读取 `captcha.code`，正常拿到结果也会 AttributeError。统一返回 `Captcha` 或统一使用字符串，并以离线假识别器覆盖调用契约。
2. **识别代理状态跨次残留**：`self.queue/results` 不清空，旧验证码请求和投票会累计；已关闭 connector 保留在 `self.conn`，下次会复用。每次调用创建局部队列/结果和正确关闭的 session；只计入 success 且结构合法的返回，处理全失败、超时、空结果。
3. **验证码重试无上限**：`RECOGNIZER_MAX_ATTEMPT=15` 未用于 `while True`。增加尝试上限与明确失败状态。
4. **配置静默失效**：`RawConfigParser()` 默认不处理行内注释，样例 `3 ; 注释` 转数值失败，被裸 except 吞掉。将样例注释移到独立行，缺字段才回退，非法值启动即报错；不要贸然全局截断含分号的密码。
5. **日志泄露**：登录错误分支直接打印 password；请求日志打印认证体和 Cookie。删除密码输出并统一脱敏。
6. **提交请求自动重试**：`client.py` 对 GET/POST 统一重试，但补选本身是有副作用的 GET。按接口语义拆分重试；提交遇到不确定结果先查已选状态，不直接重发。

### P1：根据当前页面完善定位与阶段处理

1. **多页定位**（`config.py/elective.py/loop.py`）：保留单页字段兼容，新增全页扫描或页列表；从页面分页信息发现总页数，完整查找目标，再处理“目标不存在”。当前错误把“不在配置页”直接说成“不在选课计划”。选课后课程位置可能变化，应重新定位。
2. **表格定位**（`parser.py/loop.py`）：改按必需表头识别可选表和已选表，替代 `tables[0]/tables[1]`；统一合并单元格全部文本并 strip，容忍嵌套标签和多个 class。现有 XPath 是否已失效尚不能由可访问性树判断。
3. **课程与操作分离**（`course.py/parser.py`）：保留课程号，逐步以课程号+班号（必要时附院系）定位；显式解析“刷新/补选/不可操作”及动作信息。不能假设每行操作列都有直接 `a/@href`。最终实现需真实 DOM/JS 样本。
4. **提交链接处理**（`elective.py`）：当前拼接 `scheme://host + href` 只适用于根相对链接。取得真实链接后采用基于响应 URL 的解析，验证 HTTPS、同源及允许的补选路径，保留服务端参数，不猜索引。
5. **阶段和结果模型**（`hook.py/loop.py`）：区分候补登记、正式选上、阶段未开放、跨院系未开放；当前 success 正则把“补选（或者候补）成功”放在同一状态。提交后以已选列表和选课状态复核，不只依赖一句提示。未知页面应输出明确未识别状态。

### P2：简化维护与配置语义

- `_get_refresh_interval()` 指数分布用 `1/(interval*deviation)`，默认 8、0.2 的未截断均值实际是 1.6 秒，另有 0.8 秒下限和 40 秒上限，并非文档暗示的约 8 秒。明确定义均值和抖动；未知分布名应报错。
- burst 从休息段开始，还可额外等待 300–480 秒，超出配置的休息范围；明确行为或删除隐藏等待。
- 登录连续失败 2 次即 `os.execv` 重启，妨碍累计到更高冷却阈值。用一个清晰的重试/冷却状态机，避免用进程重启恢复网络问题。
- 同步 `TTShituRecognizer.recognize()` 重复 POST 两次，第一发还在 try 外；虽非当前主路径，仍应删除重复请求并使用配置超时。
- 删除未使用的同步 recognizer 实例，或者把识别方式做成明确可选配置；增加离线配置校验和只读扫描入口，避免 import 主循环触发凭据读取/通知初始化。
- 独立核对第三方识别及通知服务协议、地址与可用性，再更新文档；本次未向这些服务发送数据。

## 5. 验证结果与后续验收

已执行：全部 `autoelective/**/*.py` 的 AST 语法解析通过；用标准库直接读取样例配置，重现 burst 数值因行内注释 ValueError。其余确定问题来自生产者/调用者及控制流静态核对，未宣称做过运行集成测试。

建议实现验收依次覆盖：纯离线配置验证；真实脱敏 HTML 的可选/已选/空列表/翻页解析；模拟识别的连续两次调用、全失败和超时；仅查询的登录及目标发现；最后另行验证真实提交结果。当前最缺的证据是操作链接、验证码请求和服务端响应契约，不是补退选列表的新 URL。

本次只新增分析文档，未修改执行代码或创建可直接运行的个人选课配置。
